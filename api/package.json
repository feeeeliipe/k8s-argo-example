{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx ts-node-dev index.ts",
    "build": "npx tsc",
    "start": "node build/index.js",
    "build:docker:blue": "docker build -t feeeeliipe/api:blue --build-arg IMAGE_COLOR=blue .",
    "build:docker:green": "docker build -t feeeeliipe/api:green --build-arg IMAGE_COLOR=green ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.6.3"
  }
}
